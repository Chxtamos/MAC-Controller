# Security

<!--
Before publishing, describe:
- Never commit Controller passwords, API tokens, private keys, or real secrets
- Keep conf.json and .streamlit/secrets.toml local only
- Use conf.example.json and secrets.example.toml as templates
- Review debug output/screenshots because REST responses may contain internal data
- Rotate any credential immediately if it was accidentally committed
- Explain how security issues should be reported to the maintainer
-->
